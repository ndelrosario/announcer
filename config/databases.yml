# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/07-Databases

all:
  doctrine:
    class: sfDoctrineDatabase
    param:
      dsn:      mysql:host=localhost;dbname=announcer
      username: root
      password: 

dev:
	doctrine:
	param:
		dsn: mysql:host=127.0.0.1;dbname=announcer_dev
		username: announcer
		password: announcer